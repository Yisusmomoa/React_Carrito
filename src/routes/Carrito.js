import React from 'react';

const Carrito = () => {
    return (
        <div>
            {/* mostrar el carrito usando reactcontext */}
            <h1>Carrito</h1>
        </div>
    );
}

export default Carrito;
